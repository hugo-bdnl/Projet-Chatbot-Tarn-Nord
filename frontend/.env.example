# URL de l'API backend (recherche sémantique). Défaut si la variable est absente :
VITE_API_URL=http://localhost:8000

# La clé d'administration (X-API-Key) ne se met PAS ici : elle se saisit dans le
# bandeau du back-office (/admin) et reste dans le navigateur de l'administrateur.
